# 学习阶段2: 配置系统详解

## 📚 本节目标

理解Undefined项目如何加载和管理配置文件,学习:
- 配置文件的格式(TOML)
- Python中的dataclass(数据类)
- 配置热更新机制
- 如何在程序中使用配置

---

## 🎯 1. 配置系统概述

### 什么是配置系统?

**配置系统**就是程序读取和管理外部配置文件的能力。

**为什么需要配置?**
- 不需要修改代码就能改变程序行为
- 可以在不同环境使用不同配置
- 方便用户自定义程序

### 配置文件位置

```
Undefined/
├── config.toml              # 主配置文件(TOML格式)
├── config.toml.example      # 配置示例文件
├── config.local.json        # 本地配置(JSON格式,自动生成)
└── config/
    └── mcp.json.example     # MCP配置示例
```

---

## 🔍 2. 配置文件格式详解

### 2.1 TOML格式

**TOML** 是一种配置文件格式,易于阅读和编写。

**基本语法**:

```toml
# 这是注释

[section]
# 键值对
key = "value"
number = 123
boolean = true

# 列表
list = [1, 2, 3]

# 嵌套
[section.subsection]
nested_key = "nested_value"
```

### 2.2 config.toml 核心配置

让我们看看配置文件的结构:

```toml
# ==================== 核心配置 ====================
[core]
bot_qq = 123456789              # 机器人的QQ号
superadmin_qq = 987654321       # 超级管理员QQ号
admin_qq = [111, 222, 333]     # 管理员列表

# ==================== 访问控制 ====================
[access]
mode = "off"                   # 访问控制模式
allowed_group_ids = []         # 允许的群号
blocked_group_ids = []         # 禁止的群号

# ==================== OneBot配置 ====================
[onebot]
ws_url = "ws://127.0.0.1:3001" # WebSocket地址
token = ""                     # 访问令牌

# ==================== AI模型配置 ====================
[models.chat]
api_url = "https://api.openai.com/v1"
api_key = "sk-xxxxx"
model_name = "gpt-4"
max_tokens = 8192
```

---

## 💻 3. Python代码解析

### 3.1 dataclass - 数据类

**什么是dataclass?**
- dataclass是Python 3.7引入的特性
- 用于创建专门存储数据的类
- 自动生成 `__init__`, `__repr__` 等方法

**基本语法**:

```python
from dataclasses import dataclass

# 定义一个数据类
@dataclass
class Person:
    name: str
    age: int
    email: str = "unknown@example.com"  # 有默认值

# 使用
person = Person(name="小明", age=18)
print(person.name)  # 输出: 小明
print(person)       # 自动生成 repr: Person(name='小明', age=18, email='unknown@example.com')
```

**项目中的应用** - `config/models.py`:

```python
@dataclass
class ChatModelConfig:
    """对话模型配置"""

    api_url: str                    # API地址(必需)
    api_key: str                    # API密钥(必需)
    model_name: str                 # 模型名称(必需)
    max_tokens: int                 # 最大token数(必需)
    queue_interval_seconds: float = 1.0  # 队列间隔(有默认值)
    thinking_enabled: bool = False  # 是否启用思考(有默认值)
```

**对比普通类**:

```python
# 普通类(需要自己写很多代码)
class ChatModelConfig:
    def __init__(self, api_url: str, api_key: str, model_name: str,
                 max_tokens: int, queue_interval_seconds: float = 1.0):
        self.api_url = api_url
        self.api_key = api_key
        self.model_name = model_name
        self.max_tokens = max_tokens
        self.queue_interval_seconds = queue_interval_seconds

    def __repr__(self):
        return f"ChatModelConfig(api_url={self.api_url!r}, ...)"

# dataclass(自动生成,代码更少)
@dataclass
class ChatModelConfig:
    api_url: str
    api_key: str
    model_name: str
    max_tokens: int
    queue_interval_seconds: float = 1.0
    # __init__、__repr__ 等方法自动生成!
```

---

### 3.2 配置加载器 - `config/loader.py`

配置加载器的核心任务:
1. 读取 TOML 文件
2. 读取 JSON 文件
3. 解析并验证配置
4. 提供给程序使用

**简化版的加载器实现**:

```python
import toml
import json
from pathlib import Path

class ConfigLoader:
    """配置加载器"""

    def __init__(self):
        self.config = {}

    def load_toml(self, file_path: str) -> dict:
        """加载TOML文件"""
        with open(file_path, "r", encoding="utf-8") as f:
            return toml.load(f)

    def load_json(self, file_path: str) -> dict:
        """加载JSON文件"""
        with open(file_path, "r", encoding="utf-8") as f:
            return json.load(f)

    def merge_config(self, toml_config: dict, json_config: dict) -> dict:
        """合并配置"""
        # JSON配置优先级更高
        merged = toml_config.copy()
        merged.update(json_config)
        return merged

# 使用
loader = ConfigLoader()
toml_config = loader.load_toml("config.toml")
json_config = loader.load_json("config.local.json")
final_config = loader.merge_config(toml_config, json_config)
```

**实际项目中的加载器** - `config/loader.py`:

这个文件有74KB,因为包含了:
- 完整的配置解析逻辑
- 配置验证(检查必需字段)
- 类型转换(字符串→int/float/bool)
- 默认值处理
- 错误处理

---

### 3.3 配置管理器 - `config/manager.py`

配置管理器的作用:
- 管理配置的生命周期
- 支持配置热更新
- 提供配置变更通知

**简化版的配置管理器**:

```python
import asyncio
from typing import Callable
from pathlib import Path

class ConfigManager:
    """配置管理器"""

    def __init__(self, config_path: str):
        self.config_path = Path(config_path)
        self.subscribers: list[Callable] = []
        self._running = False

    def load(self) -> dict:
        """加载配置"""
        # 这里调用ConfigLoader
        return ConfigLoader().load_toml(self.config_path)

    def subscribe(self, callback: Callable) -> None:
        """订阅配置变更"""
        self.subscribers.append(callback)

    async def start_hot_reload(self, interval: float = 1.0) -> None:
        """启动配置热更新"""
        self._running = True
        last_mtime = self.config_path.stat().st_mtime

        while self._running:
            await asyncio.sleep(interval)

            # 检查文件是否修改
            current_mtime = self.config_path.stat().st_mtime
            if current_mtime != last_mtime:
                print("检测到配置文件变化,重新加载...")
                new_config = self.load()
                last_mtime = current_mtime

                # 通知所有订阅者
                for callback in self.subscribers:
                    callback(new_config)

    async def stop_hot_reload(self) -> None:
        """停止热更新"""
        self._running = False
```

**使用示例**:

```python
# 定义配置变更回调函数
def on_config_change(new_config: dict):
    print(f"配置已更新: {new_config}")

# 创建配置管理器
manager = ConfigManager("config.toml")

# 订阅配置变更
manager.subscribe(on_config_change)

# 启动热更新
async def main():
    await manager.start_hot_reload(interval=1.0)

asyncio.run(main())
```

---

## 🔄 4. 配置热更新机制

### 什么是热更新?

**热更新**就是在程序运行时,自动检测配置文件变化并重新加载,不需要重启程序。

### 热更新流程图

```
程序运行中...
    ↓
监控线程定期检查(每1秒)
    ↓
检测到 config.toml 变化
    ↓
重新加载配置文件
    ↓
调用所有订阅者的回调函数
    ↓
更新内存中的配置
    ↓
程序使用新配置继续运行
```

### 项目中的热更新实现

`config/hot_reload.py`:

```python
class HotReloadContext:
    """热更新上下文"""

    def __init__(self, ai_client, queue_manager, config_manager):
        self.ai_client = ai_client
        self.queue_manager = queue_manager
        self.config_manager = config_manager

def apply_config_updates(updated: Config, changes: dict):
    """应用配置更新"""
    # 根据变更的配置项,更新对应的组件

    for key, (old_value, new_value) in changes.items():
        if key == "models.chat.queue_interval_seconds":
            # 更新队列间隔
            queue_manager.update_interval(new_value)
        elif key == "models.chat.api_key":
            # 更新API密钥
            ai_client.update_api_key(new_value)
        # ... 更多配置项的处理
```

---

## 🎮 5. 实战演练

### 练习1: 理解TOML配置

**任务**: 创建自己的配置文件

**步骤**:
1. 复制 `config.toml.example` 为 `my_config.toml`
2. 修改机器人QQ号、管理员QQ号等基本信息
3. 配置API密钥(如果有的话)
4. 观察配置文件的结构

**代码**:
```python
import toml

# 读取配置
with open("my_config.toml", "r", encoding="utf-8") as f:
    config = toml.load(f)

# 访问配置
print(f"机器人QQ: {config['core']['bot_qq']}")
print(f"API地址: {config['models']['chat']['api_url']}")
```

---

### 练习2: 使用dataclass

**任务**: 创建一个简单的配置数据类

**代码**:
```python
from dataclasses import dataclass

@dataclass
class AppConfig:
    """应用配置"""
    app_name: str
    version: str
    debug: bool = False
    max_connections: int = 100

# 使用配置类
config = AppConfig(
    app_name="MyApp",
    version="1.0.0",
    debug=True
)

print(f"应用名称: {config.app_name}")
print(f"调试模式: {config.debug}")
print(f"最大连接数: {config.max_connections}")
```

**练习**:
1. 添加 `port` 字段,默认值 `8080`
2. 添加 `log_level` 字段,默认值 `"INFO"`
3. 创建配置实例并打印所有字段

---

### 练习3: 实现简单的配置加载器

**任务**: 编写一个配置加载器

**代码**:
```python
import json
from pathlib import Path
from typing import Any

class SimpleConfigLoader:
    """简单的配置加载器"""

    def __init__(self, config_path: str):
        self.config_path = Path(config_path)

    def load(self) -> dict[str, Any]:
        """加载配置文件"""
        if not self.config_path.exists():
            raise FileNotFoundError(f"配置文件不存在: {self.config_path}")

        # 根据扩展名选择解析方式
        if self.config_path.suffix == ".json":
            return self._load_json()
        elif self.config_path.suffix == ".toml":
            return self._load_toml()
        else:
            raise ValueError(f"不支持的配置文件格式: {self.config_path.suffix}")

    def _load_json(self) -> dict[str, Any]:
        """加载JSON配置"""
        with open(self.config_path, "r", encoding="utf-8") as f:
            return json.load(f)

    def _load_toml(self) -> dict[str, Any]:
        """加载TOML配置(需要安装toml库)"""
        import toml
        with open(self.config_path, "r", encoding="utf-8") as f:
            return toml.load(f)

# 使用
loader = SimpleConfigLoader("config.toml")
config = loader.load()
print(config)
```

**练习**:
1. 实现配置验证方法 `validate()`(检查必需字段)
2. 添加获取配置值的便捷方法 `get(key, default=None)`
3. 实现配置保存方法 `save(config)`

---

### 练习4: 实现热更新监控

**任务**: 编写一个监控配置文件变化的脚本

**代码**:
```python
import asyncio
from pathlib import Path
from typing import Callable

class ConfigWatcher:
    """配置文件监控器"""

    def __init__(self, config_path: str, callback: Callable):
        self.config_path = Path(config_path)
        self.callback = callback
        self._running = False
        self.last_mtime = self._get_mtime()

    def _get_mtime(self) -> float:
        """获取文件修改时间"""
        return self.config_path.stat().st_mtime

    async def watch(self, interval: float = 1.0):
        """开始监控"""
        self._running = True
        print(f"开始监控: {self.config_path}")

        while self._running:
            await asyncio.sleep(interval)

            # 检查文件是否修改
            current_mtime = self._get_mtime()
            if current_mtime != self.last_mtime:
                print("检测到配置文件变化!")
                self.last_mtime = current_mtime

                # 调用回调函数
                if self.callback:
                    self.callback(self.config_path)

    def stop(self):
        """停止监控"""
        self._running = False

# 定义回调函数
def on_config_changed(file_path: Path):
    print(f"配置文件已更新: {file_path}")
    # 这里可以重新加载配置...

# 使用
async def main():
    watcher = ConfigWatcher("config.toml", on_config_changed)

    # 启动监控(在后台运行)
    import asyncio
    watch_task = asyncio.create_task(watcher.watch())

    # 模拟程序运行30秒
    await asyncio.sleep(30)

    # 停止监控
    watcher.stop()

if __name__ == "__main__":
    asyncio.run(main())
```

**练习**:
1. 运行这个脚本
2. 在另一个终端修改 `config.toml`
3. 观察脚本是否检测到变化

---

## 📊 6. 配置系统数据流

```
用户编辑 config.toml
    ↓
ConfigManager 监控到文件变化
    ↓
ConfigLoader 重新加载文件
    ↓
解析TOML内容为字典
    ↓
转换为Config数据类
    ↓
计算配置变化(新旧值对比)
    ↓
调用 apply_config_updates()
    ↓
更新各组件的配置
    ↓
程序使用新配置继续运行
```

---

## 💡 7. 常用配置项说明

### 7.1 核心配置 [core]

| 配置项 | 类型 | 说明 |
|--------|------|------|
| `bot_qq` | int | 机器人的QQ号 |
| `superadmin_qq` | int | 超级管理员QQ号(唯一) |
| `admin_qq` | list[int] | 管理员QQ列表 |
| `process_every_message` | bool | 是否处理每条群消息 |

### 7.2 访问控制 [access]

| 配置项 | 类型 | 说明 |
|--------|------|------|
| `mode` | str | 访问控制模式: `off`/`blacklist`/`allowlist` |
| `allowed_group_ids` | list[int] | 允许的群号 |
| `blocked_group_ids` | list[int] | 禁止的群号 |

### 7.3 模型配置 [models.chat]

| 配置项 | 类型 | 说明 |
|--------|------|------|
| `api_url` | str | API地址 |
| `api_key` | str | API密钥 |
| `model_name` | str | 模型名称(如 `gpt-4`) |
| `max_tokens` | int | 最大生成token数 |

---

## 🎓 总结

通过本节学习,你掌握了:

✅ **TOML配置格式**
- 基本语法
- 嵌套结构
- 列表和字典

✅ **dataclass数据类**
- 定义数据类
- 类型提示
- 默认值

✅ **配置加载器**
- 读取TOML/JSON文件
- 解析配置
- 验证配置

✅ **配置热更新**
- 文件监控
- 自动重载
- 配置变更通知

✅ **项目中的配置系统**
- 配置文件结构
- 配置管理器
- 热更新机制

---

**下一步**: 学习阶段3 - AI运行时系统! 🚀
