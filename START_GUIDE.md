# 启动指南

## 推荐启动方式

### 🚀 方式一：标准启动（推荐）
```bash
# 双击运行
start.bat
```
启动 NagaAgent 主程序，包含 QQ/微信机器人、语音、绘图等所有功能。

### 🔧 方式二：完整启动（带检查）
```bash
# 双击运行
start_all.bat
```
启动前会检查：
- Python 环境
- GPT-SoVITS 配置
- NapCat-Go 状态
- config.json 配置

适合第一次使用或遇到问题时。

### 🤖 方式三：带 OpenClaw 启动
```bash
# 双击运行
start_with_openclaw.bat
```
同时启动：
- OpenClaw Gateway（端口 18789）
- NagaAgent 主程序
- 自动服务检查

适合需要使用 OpenClaw 工具时。

### ⚙️ 首次安装
```bash
# Windows
setup.bat

# Linux/Mac
./setup.sh
```

## 其他脚本说明

### 启动脚本
| 脚本 | 用途 |
|------|------|
| start.bat | 标准启动 ⭐ |
| start_all.bat | 完整启动（带检查） |
| start_with_openclaw.bat | 带 OpenClaw 启动 |
| start_openclaw.bat | 仅启动 OpenClaw（显示日志） |

### 停止脚本
| 脚本 | 用途 |
|------|------|
| stop_openclaw.bat | 停止 OpenClaw Gateway ⭐ |
| stop_all.bat | 停止所有服务（OpenClaw + NagaAgent） |
| kill_all_openclaw.bat | 强制停止所有 OpenClaw 进程 |
| restart_openclaw.bat | 重启 OpenClaw Gateway |

### 测试脚本
| 脚本 | 用途 |
|------|------|
| test_openclaw_status.bat | 测试 OpenClaw 运行状态 |
| test_gateway.bat | 测试 Gateway 启动 |

### 归档脚本（在 archive/ 目录）
| 脚本 | 用途 |
|------|------|
| openclaw_manage.bat | OpenClaw 管理工具 |
| build_openclaw.* | OpenClaw 构建脚本 |
| 其他测试和检查脚本 | 已归档 |

## 目录结构

```
NagaAgent/
├── start.bat              # 标准启动 ⭐推荐
├── start_all.bat          # 完整启动（带检查）
├── start_with_openclaw.bat # 带 OpenClaw 启动
├── setup.bat              # 首次安装
├── main.py                # 主程序入口
├── config.json            # 配置文件
├── requirements.txt       # 依赖列表
├── reports/               # 报告文档目录
│   ├── 记忆系统诊断报告.md
│   ├── 定时任务修复报告.md
│   ├── OPENCLAW配置报告.md
│   └── ...其他报告
└── archive/               # 归档文件（测试脚本、临时工具）
```

## 常见问题

**Q: 应该用哪个启动脚本？**
A: 日常使用用 `start.bat`，遇到问题用 `start_all.bat` 看检查信息。

**Q: 什么时候需要 OpenClaw？**
A: 需要使用 OpenClaw 工具时用 `start_with_openclaw.bat`。

**Q: 测试脚本在哪里？**
A: 已移至 `archive/` 目录，有需要可以手动运行。
